# 🔄 Git Reset: откат изменений

Команда `git reset` позволяет откатить состояние репозитория к предыдущему коммиту.  
Она управляет тремя областями Git:
- **HEAD** — ссылка на текущий коммит
- **index (staging area)** — подготовленные файлы
- **working directory** — рабочая директория

---

## 🧠 1. `--soft`: только HEAD

```bash
git reset --soft HEAD~1
```

- Перемещает HEAD на предыдущий коммит
- Сохраняет изменения в staging area
- Рабочая директория остаётся без изменений

📝 Используется, если ты случайно сделал коммит и хочешь его "развернуть" для редактирования.

## 🧹 2. --mixed: HEAD + index (по умолчанию)

```bash
git reset --mixed HEAD^
```

- HEAD откатывается
- Снимает файлы из staging area (но оставляет их в рабочей директории)
- Рабочая директория не тронута

📝 Полезно, если хочешь пересобрать коммит с другими файлами.

## 💣 3. --hard: всё подчистую

```bash
git reset --hard HEAD^
```

- HEAD откатывается
- Снимает файлы из staging area
- Удаляет изменения из рабочей директории

⚠️ Осторожно! Это необратимая команда, теряются все несохранённые изменения.

---

### 🧾 Примеры

| Команда                   | Что делает                                       |
| ------------------------- | ------------------------------------------------ |
| `git reset --soft HEAD~1` | Откат HEAD, изменения остаются в index           |
| `git reset --mixed HEAD^` | Откат HEAD, index очищается                      |
| `git reset --hard HEAD^`  | Откат HEAD, index и рабочая директория очищаются |

---

### 📌 Визуализация (упрощённо)

```scss
--soft
[HEAD] ← index ← рабочая директория

--mixed
[HEAD]    index ← рабочая директория

--hard
[HEAD]    index    рабочая директория (все синхронизированы)
```

---

### 🧠 Полезно знать

- HEAD~1 = предыдущий коммит
- HEAD^ = то же самое, что HEAD~1
- git log — поможет найти нужный хеш коммита